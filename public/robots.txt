# During `npm run build`, a complete robots.txt (with Sitemap: your live URL) is written to `dist/`.
# This file is what Vite serves in dev. Deploy the `dist/robots.txt` from your production build.
User-agent: *
Allow: /
