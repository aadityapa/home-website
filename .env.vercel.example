# Vercel production / preview — set in Project → Settings → Environment Variables
# (or run: vercel env pull)

# Root deploy (not GitHub Pages subpath)
VITE_BASE_PATH=/

# After first deploy, set to your Vercel URL or custom domain:
# VITE_SITE_URL=https://homewebsite.vercel.app
